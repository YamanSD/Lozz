import { Button, ButtonProps } from "react-native-paper";
import Animated from 'react-native-reanimated';


/**
 * Button that springs on click
 * @constructor
 */
const SpringButton = (props: ButtonProps) => {


  return <Button {...props} />
};

export default SpringButton;
